kind = "mobile"
previewPath = "/"
title = "Estate Manager"
version = "1.0.0"
id = "artifacts/estate-manager"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 19730

[services.development]
run = "pnpm --filter @workspace/estate-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/estate-manager", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/estate-manager", "run", "serve" ]

[services.env]
PORT = "19730"
BASE_PATH = "/"
